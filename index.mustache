<!DOCTYPE html>
<html lang="en">
  {{> partials/head }}
  <body>
    {{> partials/navigation }}

    <div class="container-fluid">
      <div class="row">
        <div class="col-md-6">
          <h4>Employment History</h4>
          {{# positions}}
            {{> partials/position}}
          {{/ positions}}
        </div>

        <div class="col-md-6">
          <h4>Education</h4>
          {{# educations}}
            {{> partials/education}}
          {{/ educations}}

          <h4>Certificates</h4>
          {{# certificates}}
            {{> partials/certificates}}
          {{/ certificates}}
        </div>
      </div>
    </div>

    {{> partials/scripts}}
  </body>
</html>
